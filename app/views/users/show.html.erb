<h1><%= @user.username %>'s posts</h1> 

<% if current_user == @user %>
<%= link_to "(edit)", edit_user_path(current_user) %>
<% end %>

<h5>Posts by <%= @user.username %></h5>
<%= render @user.posts %>

<h5>Comments by <%= @user.username %></h5>

<%= render @user.comments, show_title: true %>